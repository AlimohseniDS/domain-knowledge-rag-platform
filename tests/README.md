# Tests

- `unit`: isolated module behavior
- `integration`: database, storage, queue, and provider boundaries
- `security`: tenant isolation, authorization, uploads, and deletion
- `evaluation`: retrieval and answer-quality regression suites
